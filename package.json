{
  "name": "jayantchopra-profile",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs && esbuild docs/game.mjs --bundle --format=iife --target=es2020 --charset=utf8 --outfile=docs/app.js",
    "test": "node test/game.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
